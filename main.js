function preload(){
}

function setup(){
    canvas = createCanvas(640, 480);
    canvas.position(100, 200);
    video = createCapture(VIDEO);
    circle(100, 100, 200)
    ellipse(300, 100, 55, 55);
    rect(400, 10, 55, 55);
}
